# Copy this file to .env and fill in your API keys.
# The .env file must reside in the same directory as the scripts (code_release/).
# Values are loaded into os.environ; they are never printed to stdout.

OPENAI_API_KEY=sk-...
OPENROUTER_API_KEY=sk-or-...
